# MEMORY.md - Long-Term Memory

*Curated knowledge and context that persists across sessions.*

---

## Org Knowledge

- *(document org-specific gotchas, known issues, workarounds)*

## Key Decisions

- *(track architectural and process decisions)*

## Lessons Learned

- **sfdx-git-delta:** Use as SF CLI plugin (`sf sgd source delta`), not standalone binary
- **SF CLI in containers:** Use `npm install -g @salesforce/cli@latest` on base Node image rather than slim SF CLI containers

---

*Updated as RevX learns the team's environment.*
